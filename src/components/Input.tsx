// eslint-disable-next-line react/prop-types
const Input = () => {
  return <></>;
};

export default Input;
{
  /* <input
          className="flex sm:w-72 sm:h-8 rounded-md m-auto border-gray-300 border border-solid focus:outline-none focus:ring-2 focus:ring-custom-name focus:border-transparent"
          id={id}
          placeholder=" "
          onChange={({ target }) => setValue(target.value)}
          {...props}
        />

        <label
          htmlFor={id}
          className="flex items-center justify-center text-sm font-medium text-custom-name"
        >
          {label}
        </label> */
}
